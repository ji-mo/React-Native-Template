//设计稿宽度，单位:px
export const uiWidthPx = 375;

export enum ZoomRatioEnum {
  MOBILE = 1,
  TABLET = 0.7,
  DESKTOP = 0.5,
}

export enum BreakPointEnum {
  MOBILE = 340,
  TABLET = 760,
  DESKTOP = 1080,
}
